'
'Three options to choose a file to run a macro on
'1. Select a directory and run a macro on each file
'2. Select a file and run the macro on that file
'3. Run the macro on the active workbook
'

Sub chooseDirectory()
  
End Sub

Sub chooseFile()
  
End Sub

Sub chooseActiveWB()
  
End Sub
